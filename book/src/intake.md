# Intake Contract

`gec` requires a `bic::BindingPackage` as its primary input.

Optional enrichment:
- `bic::ValidationReport` — declaration-vs-artifact validation evidence
- `bic::ResolvedLinkPlan` — resolved native link requirements
