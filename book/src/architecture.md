# Architecture

## Module Layout

- `intake` — consumes `bic::BindingPackage` and optional enrichment data
- `ir` — internal Rust projection IR
- `config` — generation configuration
- `output` — generation output container
- `error` — crate error types
